print(f'\nВведиет по отдельности три числа, которые будут написаны в строгом возрастании.\n')

number_1 = int(input('Введиет первое число: '))

while True: #начало бесконечного цикла
    number_2 = int(input('Введиет второе число: '))
    if number_2 <= number_1: #проверка на первое число меньше или равно второму
        print('🚫Ошибка. Введиет другое число. Число должно быть больше первого.')
    else:
        break #резкое прерывание цикла если условие не выполняется

while True:
    number_3 = int(input('Введиет третье число: '))
    if number_3 <= number_2: #проверяем второе чило меньше или равно третьему
        print('🚫Ошибка. Введиет другое число. Число должно быть больше второго.')
    else:
        break #резкая остановка цикла, если условие не выполняется

print(f'\nПоследовательность принята! 🎉')
