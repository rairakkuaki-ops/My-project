def is_valid_password(password):
    parts = password.split(':')
    if len(parts) != 3:
        return False
    
    a, b, c = parts
    
    if a != a[::-1]:
        return False
    
    if not is_prime(int(b)):
        return False
    
    if int(c) % 2 != 0:
        return False
    
    return True
